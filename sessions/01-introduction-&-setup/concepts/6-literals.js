/* --------------------------------------------------
   TEMPLATE LITERALS • TRUTHY / FALSY • == vs ===
   --------------------------------------------------
*/

/* 1️⃣  TEMPLATE LITERALS (back-tick strings) */
const first = 'Sai';
const last = 'Anand';

// Old-school concatenation
console.log('Hello, ' + first + ' ' + last + '!');

// Cleaner with template literals
console.log(`🌟  Hello, ${first} ${last}!  🌟`); // back-ticks + ${}
